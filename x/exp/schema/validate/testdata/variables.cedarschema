entity User {
    name: String,
    age?: Long,
    email: String
};

entity Document {
    title: String,
    public: Bool
};

action view appliesTo {
    principal: [User],
    resource: [Document],
    context: {
        ip: ipaddr
    }
};
