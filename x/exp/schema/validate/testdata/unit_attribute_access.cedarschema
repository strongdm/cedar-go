entity User {
    name: String,
    age?: Long,
    email: String,
};

entity Group {
    name: String,
};

entity Document;

action "view" appliesTo {
    principal: User,
    resource: Document,
};
